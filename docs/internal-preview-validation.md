# TrailCraft Manus 内部预览验证记录

**验证日期：2026-08-19（GMT+8）**

| 验证项 | 结果 | 观察与后续处理 |
| --- | --- | --- |
| 应用启动与基础布局 | 通过 | Manus 预览正常加载；山口箭头图标、模式切换、侧栏任务组、内部预览状态提示均可见。 |
| MapLibre 二维卫星底图 | 部分通过 | Esri 影像与归属信息出现；初次加载时有分块延迟，属于现有外部地图链路的网络行为，仍需中国大陆真实网络验收。 |
| 真实 GPX 上传 | 通过（有回退） | 原始模块 Worker 在当前预览环境未回传结果；增加 8 秒主线程回退后，`速攀129新望京20240912160539.gpx` 成功导入为“北京市 越野跑”，包含 5,038 点、13.87 km、WGS-84。地图绘制出路线，高程剖面也随之生成。 |
| 真实 FIT 与 KML 上传 | 通过（并发） | 同一批提交 `张家口市_越野跑20260710080013.fit` 与 `路线-四灵反穿20260811095617.kml` 后均导入成功。FIT 解析为 62,769 点、109.71 km、WGS-84 的实跑记录；KML 解析为 27,077 点、47.18 km、WGS-84 的规划路线。地图显示路线，高程剖面正常生成。 |
| GPX 导出 | 通过 | 当前活动路线成功下载为 `张家口市_越野跑20260710080013 (1).gpx`（7,535,462 字节）。文件以 GPX 1.1 XML 声明开头，`creator="TrailCraft"`，名称与活动路线一致，首个点的经纬度与时间戳结构完整。 |
| 三维巡游与 MP4 | 暂缓 | 为避免当前构建环境的 Cesium 资源打包造成内存终止，内部预览暂显示迁移状态；二维路线工作台优先完成。 |
| 移动端工作台布局 | 通过（基础布局） | 首轮窄屏截图显示桌面侧栏与地图并排，导致地图空间不足。现已改为默认全屏地图，右下保留“路线工具”和“展开分段表”入口；入口与内部预览状态提示错位排列，不互相遮挡。点击路线工具将打开可滚动的底部任务抽屉；仍需在真实手机浏览器验证外部瓦片加载速度与触控手势。 |

> 此记录仅用于内部迁移验证。地图服务、轨迹位置数据与公开传播边界未完成合规核验，项目不得主动对外发布。
